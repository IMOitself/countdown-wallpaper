# countdown-wallpaper
add a countdown text to your image and set it as wallpaper.

the countdown is currently set to **november 29.**


## preview
soon..

## step 1: install pillow
if you haven't already, open a terminal (powershell or command prompt) and run:
```
pip install Pillow
```

## step 2: run it
2. open the `RUN.bat` file

## customizations
feel free to modify the code like setting a different date, positioning the text and changing the wallpaper image.
<br>
imma not give u a step by step process. if u know python then `make_wallpaper.py` will be very easy for u to modify:D

> [!TIP]
> optionally, u can open `wallpaper_design.psd` to edit the black windows logo design on photopea.com
